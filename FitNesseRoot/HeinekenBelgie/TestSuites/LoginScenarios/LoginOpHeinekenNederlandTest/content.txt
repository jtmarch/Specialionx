!include -c <HeinekenSharedContext.SharedIncludes.SharedScenarios.StartUpInclude

!include -c <HeinekenSharedContext.SharedIncludes.SharedScenarios.SharedLoginScenario

!| script | selenium driver fixture |
| Succesvolle login test |

!| script | selenium driver fixture |
| Foutieve email bij login |

!contents -R2 -g -p -f -h